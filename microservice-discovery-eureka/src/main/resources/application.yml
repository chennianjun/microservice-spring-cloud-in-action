server:
  port: 8761
eureka:
  client:
    fetch-registry: false
    register-with-eureka: false
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true
#eureka.client.healthcheck.enable: true #应用程序就会将自己的健康状态传播到Eureka Server